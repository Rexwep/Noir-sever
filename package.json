{
  "name": "noir-x11-cms",
  "version": "1.0.0",
  "private": true,
  "description": "NOIR X11 site + CMS admin panel (zero dependencies, Node built-in sqlite).",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "reset-password": "node server.js reset-password"
  }
}
